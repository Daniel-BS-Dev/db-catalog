import './styles.scss';

const Button = () => {
    return (
        <button className="btn btn-primary button">
            <span>inicie agora sua busca</span>
        </button>
    );
} 

export default Button;